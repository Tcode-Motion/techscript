# techscript_stdlib

Extended library modules containing `io`, `math`, `string`, `file`, `web`, `time`, and `json` APIs for TechScript 2.0.

## Contents
- `StdModule`: Mappings for standard library functions.
- `StdlibRegistry`: Catalog loading hook interface.
