[package]
name = "techscript_errors"
version = "2.0.0"
edition = "2021"
description = "Unified error reporting and terminal diagnostics renderer for TechScript 2.0"

[dependencies]
techscript_common = { path = "../common" }
serde = { workspace = true }
