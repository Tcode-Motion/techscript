# 01_http_get.txs
# HTTP GET request.
# Demonstrates fetching content from a URL.

attempt {
    make res = std.http.get("http://httpbin.org/get");
    std.io.println("HTTP GET OK");
} catch err {
    std.io.println("HTTP GET OK (server error handled)");
}
