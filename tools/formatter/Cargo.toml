[package]
name = "techscript_formatter"
version = "0.1.0"
edition = "2021"
description = "Code formatter (fmt) implementing layout specs for TechScript 2.0"

[dependencies]
techscript_common = { path = "../../compiler/common" }
techscript_syntax = { path = "../../compiler/syntax" }
techscript_ast = { path = "../../compiler/ast" }
