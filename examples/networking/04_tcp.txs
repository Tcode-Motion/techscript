# 04_tcp.txs
# TCP sockets example.
# Demonstrates connecting to a local port.

attempt {
    make conn = std.net.tcp_connect("127.0.0.1", 8080);
    std.io.println("TCP socket checked.");
} catch err {
    std.io.println("TCP socket checked. (connection failed as expected)");
}
