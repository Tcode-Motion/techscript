# 05_classes.txs
# Classes example.
# Demonstrates defining a model and instantiating it.

model User {
    make id = 1;
    make name = "Alice";
    
    build greet() {
        return "Hello from " + self.name;
    }
}

make u = new User();
std.io.println(u.greet());
