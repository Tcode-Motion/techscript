# techscript_lexer

DFA-based token scanner implementing unicode literal rules and keyword lexing for TechScript 2.0.

## Contents
- `Lexer`: Scans source buffers and produces token vectors.
- `lex()`: Helper utility to perform scanning on a source string.
