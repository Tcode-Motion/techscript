# techscript_builtins

Native callbacks and standard environment functions for the TechScript 2.0 runtime.

## Contents
- `NativeCallback`: Function pointer binding for native calls.
- `BuiltinRegistry`: Context loader mapping `say()`, `ask()`, and `len()` calls.
