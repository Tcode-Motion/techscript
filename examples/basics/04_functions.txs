# 04_functions.txs
# Functions example.
# Demonstrates function declarations and calling.

build add(a, b) {
    return a + b;
}

make result = add(5, 7);
std.io.println("Result: " + std.strings.from_int(result));
